{
  "name": "herdr-go-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "herdr-go mobile-first web UI (live terminal mirror)",
  "scripts": {
    "dev": "vite",
    "bundle": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@xterm/xterm": "^5.5.0",
    "jsdom": "^25.0.0",
    "playwright": "^1.62.1",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
