{
  "name": "herdr-gateway-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "herdr-gateway mobile-first web UI (xterm.js live terminal)",
  "scripts": {
    "dev": "vite",
    "bundle": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@xterm/xterm": "^5.5.0",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/addon-web-links": "^0.11.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0",
    "jsdom": "^25.0.0"
  }
}
