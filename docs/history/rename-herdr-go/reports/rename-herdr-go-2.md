[DONE]

Corrected the promoted path: demo is loopback-only by default, installed agents can write user workspaces, and unsupported systemd environments fail before mutation with accurate repeat-login guidance.

Files touched: `README.md`, `docs/installation.md`, `docs/usage.md`, `src/main.rs`, `install.sh`, `packaging/herdr-go.service`, `tests/rename_contract.sh`, `docs/specs/installation.md`, `docs/specs/system-overview.md`.

Full trace and verification evidence: [rename-herdr-go-2 cell](../../../../.bee/cells/rename-herdr-go-2.json).
