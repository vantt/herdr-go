# self-update-merge-config-14

[DONE] — Hidden `--internal-merge-config` CLI verb wired into `main.rs`, dispatching to a new `run_internal_merge_config` in `src/config/merge.rs`; `print_help()` unchanged.

Files touched: `src/main.rs`, `src/config/merge.rs`

Full trace and evidence: `.bee/cells/self-update-merge-config-14.json`
