# pbi-033-service-lifecycle-cli-2

**Status:** [DONE]

**Outcome:** Documented `herdr-go service {start|stop|restart|status}` as the recommended unified command in README.md and docs/installation.md, with the existing raw per-platform (`systemctl --user` / `launchctl` / Scheduled Task) commands kept alongside as a fallback reference table, per D8. Fixed a stale cross-reference to the renamed "Restart" heading in README.md as an in-scope deviation.

**Files touched:** README.md, docs/installation.md

Full trace/evidence: `.bee/cells/pbi-033-service-lifecycle-cli-2.json`
