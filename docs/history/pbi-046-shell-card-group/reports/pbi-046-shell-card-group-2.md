# pbi-046-shell-card-group-2

[DONE] — `renderShellRow` gets a leading `.shell-icon` glyph (D3) and `.shell-row` gets a solid-black CSS override (D4); `.agent-card` rule untouched.

Files touched: `web/src/views/switcher.ts`, `web/src/styles.css`, `web/test/switcher.test.ts`

Full trace and verification evidence: `.bee/cells/pbi-046-shell-card-group-2.json`
