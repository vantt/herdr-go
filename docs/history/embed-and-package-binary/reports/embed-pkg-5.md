# embed-pkg-5 report

**[DONE]**

Outcome: Updated README.md, docs/installation.md, and docs/deployment.md so they describe the real download-first/build-fallback `install.sh` behavior (D 3168932d) and the embedded-by-default web UI with its always-succeeding `doctor.rs` check (D b300856d), instead of the pre-embedding text.

Files touched: `README.md`, `docs/installation.md`, `docs/deployment.md`.

Full trace/evidence: `.bee/cells/embed-pkg-5.json`.
