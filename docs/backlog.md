# Product Backlog

PBI cho session khác nhận làm. Status: `proposed` → `in-flight` → `done`.

| ID | PBI | Status | Ghi chú |
|---|---|---|---|
| PBI-022 | **Tạo shell / agent mới từ điện thoại**: 1 FAB trên switcher mở 1 sheet — chọn đích (workspace + folder của nó), rồi `Shell` hoặc 1 hàng cho mỗi agent preset. Gọi `tab.create` và `agent.start` của herdr, luôn truyền `cwd` tường minh. Chỉ tạo được bên trong workspace đã mở sẵn; đổi folder bằng cách gõ `cd` qua panel Type có sẵn. Đây là khả năng **ghi** đầu tiên của app ngoài việc reply vào pane đang chạy. | in-flight | Feature `new-shell-new-agent` (docs/history/new-shell-new-agent/CONTEXT.md, D1-D11) khoá toàn bộ hình dạng sản phẩm. Backend đã xong và đóng: `web-create-endpoints` (slice 4, P1-P10) — spec ở `docs/specs/web-api.md`. `web-create-sheet` (slice 5) Phase 1 + Phase 2 đã xong và đóng: FAB + sheet chạy full end-to-end (destinations, caveat, disambiguation khi trùng label+folder, Shell/preset, lỗi inline, điều hướng thẳng vào terminal mới) — spec ở `docs/specs/create-sheet.md` (coverage full trừ ảnh chụp màn hình), `switcher.md`, `terminal-detail.md`. Còn lại: review độc lập cho cả slice 4 và slice 5, chưa làm. |
| PBI-028 | **Research: publish herdr-gateway như 1 plugin trong hệ sinh thái herdr**: tìm hiểu herdr có cơ chế plugin chính thức không (tài liệu: https://herdr.dev/docs/plugins/, và tìm thêm trên GitHub repo của herdr nếu tài liệu chưa đủ) — hình dạng plugin là gì (manifest, entry point, cách đăng ký), có publish registry/marketplace không, ràng buộc gì (protocol version, sandboxing, permissions). Output: quyết định có khả thi không + hình dạng kỹ thuật cần để làm PBI-029. | proposed | Người dùng yêu cầu trực tiếp. Domain type RUN/research — nên đi qua `bee-xia` (research scout) khi bắt đầu, không tự đoán cấu trúc plugin. |
| PBI-029 | **Triển khai + publish herdr-gateway như plugin herdr**: dựa trên kết quả PBI-028, implement đúng hình dạng plugin herdr yêu cầu và publish lên registry/kênh phân phối chính thức của herdr (nếu có). | proposed | Phụ thuộc PBI-028 — chưa có hình dạng kỹ thuật thì chưa lên plan được. Người dùng yêu cầu trực tiếp. |
| PBI-027 | **Bàn phím OS che mất đáy terminal ở màn hình terminal-detail**: bấm Type mở form nhập liệu, `applySheetInset` (`web/src/views/terminal.ts:189-193`) đã reserve đúng khoảng trống cho form đó (`sheet.offsetHeight`) — nhưng khi bàn phím ảo của OS bật lên sau đó (trigger bởi `replyText.focus()`), một số browser lại che tiếp phần đáy terminal phía trên form, phải cuộn lên mới thấy. **Thứ tự hiển thị đúng phải là (từ trên xuống): đáy terminal → typing form → bàn phím OS** — không cái nào bị cái phía dưới che khuất. Code hiện không lắng nghe `window.visualViewport` (API chuẩn để biết chiều cao thật của bàn phím OS) ở đâu cả, nên chỉ tính được khoảng trống cho form của app, không tính được khoảng trống bàn phím OS chiếm thêm. | proposed | Người dùng phản ánh trực tiếp, đã confirm lại mô tả. Cần tìm hiểu cách xử lý tốt hơn scroll thủ công — hướng khả dĩ: lắng nghe `window.visualViewport.resize`/`scroll` để biết chiều cao bàn phím thật, cộng vào `paddingBottom` cùng lúc với `sheet.offsetHeight`. |
| PBI-025 | **Terminal detail thiếu URL riêng, refresh bị đá về home**: click từ home vào 1 pane/agent hiện chỉ đổi state trong bộ nhớ (`web/src/main.ts`'s `Route` type: `{name:"terminal"; agent}`), không gọi `history.pushState`/đổi URL gì cả — cả file không có 1 lệnh `history.`/`location.` nào. Refresh (F5) luôn chạy lại `bootstrap()` từ đầu, chỉ biết `login`/`switcher`, không có cách khôi phục lại đúng pane đang xem → mất ngữ cảnh, đặc biệt khó chịu trên mobile (dễ refresh nhầm/pull-to-refresh). | proposed | Người dùng phản ánh trực tiếp. Cần 1 URL định danh được pane (vd `?pane=<id>` hay route path) + đọc lại URL đó lúc `bootstrap()` để mở thẳng terminal detail thay vì switcher. |
| PBI-026 | **Chuẩn hoá thuật ngữ pane/agent/terminal cho trang detail**: cùng 1 khái niệm (màn hình mở ra khi click từ home) đang được gọi khác nhau ở nhiều chỗ — `docs/specs/terminal-detail.md` tự nó đã lẫn lộn ("agent list", "selected agent's terminal detail", trạng thái `Pane gone`, "selected terminal"); code gọi field là `agent: AgentRow` (`web/src/main.ts`) dù chính PBI-024 vừa chỉ ra app đang thu hẹp theo `agents[]` trong khi herdr gốc là pane-centric (`panes[]`). Cần rà lại toàn bộ (spec + code + UI text) xem nên chuẩn hoá gọi là gì, tránh 3 từ trộn lẫn gây khó hiểu khi đọc code/spec sau này. | proposed | Người dùng phản ánh trực tiếp. Liên quan PBI-024 (cùng câu hỏi pane-vs-agent, đang mở ở feature `new-shell-new-agent`) — nên rà cùng lúc hoặc ít nhất tham chiếu quyết định cuối cùng của PBI-024 khi làm PBI này. |
| PBI-020 | **Tạo workspace cho project chưa mở**: thêm mục "Project khác..." vào dropdown đích — duyệt thư mục con của `allowed_roots`, gọi `workspace.create{cwd,label}` rồi mới tạo shell/agent. Cần endpoint duyệt thư mục mới + audit bảo mật (lộ cấu trúc thư mục qua web). | proposed | Deferred idea từ `new-shell-new-agent` (D8, D9 đã chừa sẵn chỗ trong dropdown). **Đây là câu hỏi sản phẩm, không phải nợ kỹ thuật**: herdr desktop cũng không chọn folder được — nó spawn từ cwd của pane đang active rồi người dùng `cd`. Làm PBI này nghĩa là mobile mạnh hơn desktop, cần người quyết có muốn thế không. |
| PBI-021 | **"New shell here" từ màn hình terminal detail**: tạo shell ngay trong workspace của pane đang xem, không cần về switcher chọn đích. | proposed | Deferred idea từ `new-shell-new-agent` (D1). Đây là chỗ nhiều ngữ cảnh nhất để tạo, nhưng vì FAB đã prefill đích theo `focused_workspace_id` nên chỉ tiết kiệm 1 chạm, đổi lấy code path UI thứ hai. Cân nhắc lại sau khi dùng thật slice 1. |
| PBI-019 | **Non-release install-flow smoke (fast feedback)**: chạy nhánh cài đặt thật của `install.ps1`/`install.sh` trong `ci.yml` (mỗi push) nhắm vào 1 asset build local giả lập, thay vì chỉ có bằng chứng ở `release.yml` sau khi đã publish thật. | proposed | Deferred idea từ `windows-installer-runtime-smoke` (D7) — feature đó chỉ chứng minh luồng download+install thật trên asset public sau publish; bản CI nhanh hơn dùng asset giả cần scope riêng vì test 1 thứ khác (logic script, không phải publish path thật). |
| PBI-023 | **macOS Herdr round-trip runtime smoke**: launchd-equivalent của `scripts/windows-runtime-smoke.ps1` — chứng minh binary herdr-go thật sự nói chuyện được với 1 bản Herdr thật trên macOS CI (login, agent list, pane screen/input, subscription). macOS hiện chưa có test nào chứng minh việc này (`ci.yml`'s `macos` job chỉ chạy fmt/clippy/test). | proposed | Deferred idea từ `macos-installer-runtime-smoke` (D1) — feature đó chỉ chứng minh installer lifecycle (install.sh → launchd → crash-restart → uninstall) qua `/api/health`, không wire Herdr binary vào LaunchAgent, để giữ scope nhỏ đối xứng với PBI-018. (Đánh số lại từ PBI-020 ban đầu — trùng ID với `new-shell-new-agent`'s PBI-020 do phiên concurrent khác đặt cùng lúc.) |
| PBI-015 | **Contributor dev-as-live deployment cho macOS/Windows**: tương đương `dev-deploy.sh` (build từ checkout, chạy như service) cho macOS (launchd) và Windows (Scheduled Task), hiện chỉ có trên Linux. | proposed | Deferred idea từ `cross-platform-install` (D7) — feature đó chỉ làm no-clone install cho end-user, không làm dev-deploy parity. |
| PBI-016 | **Hỗ trợ Intel Mac (`x86_64-apple-darwin`)**: thêm target vào release matrix + chứng minh nhánh Darwin của `install.sh` chạy đúng trên Intel. | proposed | Deferred idea từ `cross-platform-install` (D11) — release matrix hiện chỉ publish `aarch64-apple-darwin` (Apple Silicon); comment cũ trong release.yml nói "Intel macs build from source via install.sh's cargo path" nhưng install.sh chưa có path đó thật. |
| PBI-008 | **Pane-level rename qua web UI**: cho phép rename pane từ herdr-go (gọi thẳng pane-rename socket method của herdr), thay vì chỉ đổi được qua herdr TUI. | proposed | Deferred idea từ `terminal-workspace-org` CONTEXT.md — cần người quyết có nên duplicate workflow herdr-native này trong web UI hay không trước khi lên plan. |
| PBI-009 | **Multi-machine aggregation**: gộp hiển thị nhiều herdr instance (nhiều máy) trên cùng 1 home view — structure `machine/workspace/tab/pane`. | proposed | Deferred idea từ `terminal-workspace-org` (D5) — KHÔNG thiết kế bây giờ, chưa có máy thứ 2 để test. Điểm khởi đầu nếu làm sau: field `machine_label` scalar (xem CONTEXT.md D5). |
| PBI-002 | **M0 spike — Tier 2 `observe`/`control` trên mobile** (PRD §10 [CAO] #2): đo độ trễ frame qua mạng di động, reconnect full-frame resync theo `seq`, `--takeover` khi cùng agent mở nhiều thiết bị, resize khi xoay ngang/dọc phone. Trục chính của app — kết quả quyết định kiến trúc relay WebSocket↔observe/control. Bằng chứng vào `.bee/spikes/`, delta ghi `docs/DISCOVERY.md`. | done (protocol layer) | **GO** — relay khả thi, §10 #2 de-risk ở protocol layer (bằng chứng `.bee/spikes/tier2-observe-control/findings.md`, delta `docs/DISCOVERY.md`). Số liệu latency/UX thực **hoãn** sang live test khi build §11 bước 4. 1 quyết định mở: resize observer-vs-controller khi xoay phone (chốt trước bước 4). |
| PBI-024 | **Home bỏ sót workspace không có agent**: trang home chỉ liệt kê workspace có agent đang chạy; workspace mà mọi pane đều là shell/cmd thì vô hình. Người dùng phản ánh, và bản chụp herdr thật đã commit xác nhận: `wB` có 1 pane shell, 0 agent — **và `wB` chính là `focused_workspace_id`**, tức workspace đang được dùng lại là cái không hiện. | done | Feature `home-shell-workspaces` (docs/history/home-shell-workspaces/CONTEXT.md, D1-D7). Mỗi shell pane trong workspace 0-agent giờ hiện thành 1 card riêng trên home (không badge trạng thái, tap mở thẳng terminal), header nhóm cũng ẩn badge khi cả nhóm chỉ toàn shell (tránh lộ giá trị `Unknown` thật của herdr). Spec: `docs/specs/switcher.md`, `web-api.md`. "Add New không hoạt động đúng chỗ" đã fix trước đó qua `web-create-sheet`. Chưa review độc lập. |
| PBI-018 | **Windows installer runtime smoke test**: script chạy trên `windows-2022` CI thật, download release ZIP → chạy `install.ps1` → xác nhận Scheduled Task thực sự start + restart sau crash → chạy `install.ps1 -Uninstall` → xác nhận dọn sạch. Đối xứng với PBI-017 (macOS) và `scripts/windows-runtime-smoke.ps1` đã có cho windows-support. | done | Feature `windows-installer-runtime-smoke` (docs/history/windows-installer-runtime-smoke/), 1 cell capped: `scripts/windows-install-smoke.ps1` mới + step post-publish trong `release.yml`'s `release-windows` job. Verify local (YAML hợp lệ, cargo/clippy/fmt/rename-contract/web suite xanh) đã chạy — máy dev không có `pwsh` nên logic PowerShell/Scheduled-Task thật chỉ được chứng minh khi release tag thật tiếp theo chạy CI (D12). Chưa review độc lập. |
| PBI-017 | **macOS runtime smoke test**: script chạy trên `macos-14` CI thật, download release archive → chạy `install.sh` → xác nhận launchd agent thực sự start + supervise process (kể cả restart sau crash) → chạy `install.sh --uninstall` → xác nhận dọn sạch. Đối xứng với `scripts/windows-runtime-smoke.ps1` đã có cho Windows. | done | Feature `macos-installer-runtime-smoke` (docs/history/macos-installer-runtime-smoke/), 1 cell capped: `scripts/macos-install-smoke.sh` mới + job `macos-install-smoke` riêng (needs: build) trong `release.yml`. Verify local (bash -n cú pháp sạch, YAML hợp lệ, cargo/clippy/fmt/rename-contract/web suite xanh) đã chạy — máy dev không có macOS/launchctl nên logic launchd thật chỉ được chứng minh khi release tag thật tiếp theo chạy CI (D12). Herdr round-trip proof cho macOS tách sang PBI-023 (out of scope, D1). Chưa review độc lập. |
| PBI-003 | **Live probe `pane.agent_status_changed`** (residual PBI-001, DISCOVERY mục 1): dựng workspace cô lập + agent thả ràng, lái idle→working→blocked→done; đo latency delivery qua subscribe, xác nhận duplicate 2×/1ms và cursor de-dup. Điều kiện để chuyển notify từ poll sang subscribe — **không chặn ship M0** (notify poll vẫn đúng thiết kế). Bằng chứng vào `.bee/spikes/`, delta ghi `docs/DISCOVERY.md`. | done | Feature `pane-agent-status-changed-live-probe`. Live-probe thật trên herdr 0.7.4 (session cô lập, không đụng `default`) — latency 11-18ms (nhanh hơn poll 500ms hiện tại ~30-40x); duplicate 2×/1ms KHÔNG tái hiện được (thay vào đó phát hiện hành vi coalescing thật ở nhịp ~100ms khi update dồn dập); `done` xác nhận KHÔNG bao giờ tự xuất hiện qua subscribe — luôn báo `idle`, consumer phải tự suy ra "done" từ `idle` giống code CLI hiện tại. **Khuyến nghị: qualified GO** cho việc migrate poll→subscribe sau này (kèm 2 caveat), nhưng bản thân PBI-003 không migrate `src/watcher.rs` — đó là scope riêng nếu được ưu tiên sau. Delta đã ghi vào `docs/DISCOVERY.md`. Chưa review độc lập. |
| PBI-013 | **`doctor` thành config surface thân thiện**: nâng `herdr-go doctor` từ chẩn đoán read-only thành 1 lệnh tương tác — mỗi check lỗi đề nghị fix ngay tại chỗ, và cho phép sửa mọi setting `config.json` (8 field) + set up secrets (web/github/telegram token vào `herdr-go.env` mode 600, token không bao giờ vào config.json). Giữ nguyên hand-edit; non-TTY vẫn chạy read-only như cũ. Cập nhật tài liệu hướng dẫn sau khi xong. | done | Feature `doctor-config-surface` (docs/history/doctor-config-surface/CONTEXT.md) — 3 slice đều capped: Slice 1 (module split + write/repair/secret foundations), Slice 2 (three-phase diagnose→fix→recheck, guided fixes, all-8-field + 3-secret settings editor, `--check` flag), Slice 3 (docs: `docs/specs/doctor.md` mới, `docs/advanced/configuration.md`/`troubleshooting.md` cập nhật). Spec: `docs/specs/doctor.md`. Verified, chưa qua independent review (đã đăng ký review candidate). |
| PBI-012 | **Windows lifecycle integration**: PowerShell installer, Windows Service/auto-start, uninstall/upgrade và dev deployment cho herdr-gateway. | done | Slice 2 (Windows) của `cross-platform-install` đã capped: `install.ps1` (irm\|iex, không cần admin), Scheduled Task logon-trigger (không phải elevated NT Service), `-Uninstall`. Persona-panel bắt 1 CRITICAL trước khi chạy (script tự tạo token file sẽ kế thừa ACE SYSTEM/Administrators, khiến binary tự chặn khởi động ở lần chạy sau) — sửa tận gốc: script không đụng token file, để binary tự tạo+bảo vệ như đã có sẵn. Dev deployment cho Windows tách sang PBI-015 (out of scope). Chưa có smoke test end-to-end thật trên Windows runner — xem PBI-018. |
| PBI-014 | **macOS lifecycle integration**: no-clone installer (curl one-liner, mở rộng `install.sh`), launchd user-agent auto-start, native `~/Library/Application Support` paths, uninstall/upgrade cho herdr-go trên macOS. | done | Slice 1 (macOS) đã capped: native path resolution + launchd plist + install.sh Darwin branch + `--uninstall` + release packaging swap + CI macOS job — tất cả verify xanh, path resolution và packaging đã chứng minh thật trên macOS-14 runner (2 lần chạy CI/release thật, tag test xóa ngay sau). Chưa có smoke test tự động cho toàn bộ luồng cài đặt thật (download→install→launchd start→uninstall) — xem PBI-017. |
| PBI-011 | **Hỗ trợ chạy trên Windows**: research các khác biệt nền tảng và triển khai để herdr-gateway build, đóng gói, cấu hình và chạy foreground trên Windows; mức support chỉ được claim theo bằng chứng runtime. Phạm vi gồm IPC/kết nối herdr, native per-user state + token protection, target-specific ZIP, CI Windows và tài liệu đúng với artifact. | done | Full support (không phải preview): real-herdr foreground proven trên Windows Server 2022 CI thật (decision `86491143`). Target-specific ZIP giờ được publish thật qua release job riêng (`release-windows`, tách khỏi job `build` chung Linux/macOS), tự re-prove trước khi package, không kèm install.sh/systemd. `docs/history/windows-support/`, `docs/history/windows-release-matrix/`. |
| PBI-M1 | **M1 — gateway foundation + web terminal MVP**: Rust backend (config, security, herdr port + Fake/CLI, store, supervisor, watcher, web auth+switcher+Tier2 relay, notify) + mobile-first xterm.js UI + installer + docs. | done | 9 cells capped; 78 Rust tests (incl. 4 e2e) + 15 web tests green; clippy -D warnings clean; `herdctl --demo` serves the app end-to-end. Feature m1-gateway-web-terminal. |
| PBI-001 | **M0 spike — verify `events.subscribe`** (PRD §10 [CAO] #1): chạy herdr thật, test subscribe giao đủ event, không miss, không drop khi reconnect, xác nhận duplicate-event (`agent_status_changed` 2 lần/1ms) và cách de-dup. Kết quả quyết định Event watcher giữ poll 500ms hay nâng lên subscribe. Bằng chứng vào `.bee/spikes/`, delta ghi `docs/DISCOVERY.md` (discovery thắng spec). | done | **Verdict: subscribe reliable → watcher CÓ THỂ nâng poll→subscribe.** Live-verified herdr 0.7.4/proto 16. Bằng chứng `.bee/spikes/pbi-001-events-subscribe/`, delta `docs/DISCOVERY.md` (D: one-req/conn, replay ring buffer, per-pane pane_id, 2 envelope). Notify vẫn poll cho M0 (Telegram-B). Residual: đo agent_status_changed với agent thật. |
| PBI-004 | **Zero-config default chạy được ngay** + `herdctl doctor`: no-arg `herdctl` tự tạo config mặc định + token bền vững (mode 600), chạy thẳng vs herdr thật; `herdctl doctor` chẩn đoán setup (herdr/socket/protocol/config/token/UI/bind) với fix từng dòng. | done | Feature run-ready-dev-deploy (rrd-1, rrd-3). Live-verified: no-arg boot auto-config+token, doctor 9 checks ✓. |
| PBI-005 | **Dev-as-live in-place deploy** + installation guide: `dev-deploy.sh` + `herdr-gateway-dev.service` chạy thẳng build output repo (compile xong = live, không copy); `docs/installation.md` zero-to-running. | done | Feature run-ready-dev-deploy (rrd-2, rrd-4). Live: dev service active, phục vụ herdr thật (7 agents). |
| PBI-006 | **Terminal naming trong home list**: hiện home chỉ show theo `description`, khó hình dung terminal nào là gì. Đặt tên/hiển thị dùng cả workspace + tab + pane name (không chỉ description) để list dễ nhận diện hơn. | done | Feature `terminal-workspace-org` (docs/history/terminal-workspace-org/), spec `docs/specs/switcher.md`. Tab label hiện luôn dưới dạng caption phụ; workspace tên hiện qua header của group (chỉ khi >1 workspace) — gộp D3+D4 thành 1 cơ chế hiển thị. 4 cell capped, verify độc lập (Rust 83 test + clippy sạch; web 4 file/18 test + typecheck + bundle sạch). Chưa review độc lập. Liên quan PBI-007 (cùng feature). |
| PBI-007 | **Tổ chức lại home theo workspace**: cho phép sắp xếp/filter home list theo workspace, không chỉ liệt kê phẳng. Structure đề xuất `machine/workspace/tab/pane` để dọn đường cho khả năng mở rộng sau này — hiển thị herdr từ nhiều máy trên cùng 1 home. | done | Feature `terminal-workspace-org` (docs/history/terminal-workspace-org/), spec `docs/specs/switcher.md`. Group theo workspace bằng collapsible section, sort alphabet, header dùng status rollup herdr đã tính sẵn, mặc định expand, collapse-state chỉ trong phiên. "Machine" concept KHÔNG build (YAGNI, D5) — xem PBI-009. Chưa review độc lập. Liên quan PBI-006 (cùng feature). |
| PBI-010 | **Thẻ pane dễ nhận diện hơn**: user không phân biệt được pane nào đang làm gì chỉ nhìn lướt qua home list — title bị cắt 1 dòng, không đủ đọc. Đổi title sang 2 dòng, gộp kind+tab thành 1 caption, thêm watermark monogram theo `kind` (màu hash ổn định, không dùng logo hãng thật) để nhận diện nhanh bằng mắt thay vì phải đọc chữ. | done | Feature `agent-card-legibility` (docs/history/agent-card-legibility/), spec `docs/specs/switcher.md`. 1 cell capped, verify độc lập (4 file/20 test pass, typecheck sạch). Chưa có screenshot xác nhận layout mới bằng mắt — cần user xem lại trên máy. Chưa review độc lập. |
